# 毛绒玩具项目 · 待交付 TODO

**更新**：2026-09-08 · 眼睛外观已验收并停止专项排查，C1/C2 软件实现完成，等待其余实机确认
**当前进度**：见 `STATUS.md`
**分支**：`feat/plush-toy-design`（C1/C2 已本地提交 `5693891`，尚未推送）

---

## A. 实机外观确认

### A1. 虹膜颜色 → 定稿 RGB_ORDER —— ✅ 已确认
**实机结论（2026-09-07）**：用户确认当前眼睛显示正常，保留现有 `DISPLAY_RGB_ORDER`，无需红蓝互换。

**已铺好的退路**：即使不改代码，联网后对玩具说「眼睛颜色反了」，MCP 工具 `self.eyes.swap_colors` 会翻转并存进 NVS，重启不丢。

### A2. 眨眼同步 —— ✅ 接受当前表现，不再专项排查
**结论（2026-09-08）**：已从“顺序传输”改为“按条交错”，理论最大偏差从 66ms 降到 6ms；用户确认眼睛当前表现正常，并明确要求停止继续排查。保留已实机稳定的 10MHz，不为追求未经观察到的改善上探 20MHz。

这里记录的是用户对当前整体表现的验收，不冒充示波器或逐帧录像测量结果；以后只有在再次观察到可见错位时才重开该项。

### A3. 眼睛造型是否满意 —— ✅ 已确认
**实机结论（2026-09-07）**：用户查看真屏后确认“眼睛现在好了”，保留当前巩膜比例、瞳孔大小和高光位置。

---

## B. 联网链路验收（B1 已由既有日志补证，B2/B3 仍需设备联网）

**前置**：手机热点开启 → Mac 连上该热点 → 确认 Mac 的 IP（热点重开会变）→ 设备配网填 `http://<MacIP>:8003/xiaozhi/ota/` → 服务端重启（`data/.config.yaml` 的 prompt 改动需重启才生效）

**2026-09-08 核对**：服务端已运行并监听 OTA `:8003`、WebSocket `:8010`；最后一次设备连接使用旧地址 `172.20.10.14`，Mac 当前地址为 `10.35.80.13`，所以暂时无法继续 B2/B3 的现场验收。

### B1. emoji 白名单是否生效 —— ✅ 已验证

**静态核对**：服务端 `EMOJI_MAP`、`data/.config.yaml` prompt 和固件 `kPresets` 均为 21 项，双向无缺失；服务端日志确认进程在 prompt 修改后重启。

**运行证据（2026-09-07 21:36–21:38）**：重启后的同一设备会话中，模型两次回复分别以 `😂`（`funny`）和 `😌`（`relaxed`）开头，均在白名单内且不再恒定回落为 `happy`。可用日志样本只有这两条，不能把概率型 prompt 宣称为形式化的“永远不会越界”；但已足以证明配置被加载并能驱动不同的有效情绪。

**残余风险**：模型曾自发使用 😊（U+1F60A），它**不在** `EMOJI_MAP` 中，会静默回落成 `happy`；而 🙂（U+1F642）才在表内。两者肉眼几乎无差别。prompt 已明写「形近的其他 emoji 一律无效（例如 😊 就不行，必须用 🙂）」；现有样本已通过，若以后再次越界再改成服务端确定性校验。

### B2. emotion → 表情 + 手势联动
“开心一下”这一轮已有 `😂` 回复和 `self.limbs.cheer` 调用，设备 MCP 返回 `true`，证明模型选择与设备 RPC 入队链路接通。仍需现场观察 `funny` 眼型和 cheer 手势是否同时呈现；RPC 成功不能替代机械动作与真屏验收。

### B3. 手势工具的其余两个
`self.limbs.hug` 已端到端实机验证通过。`cheer` 已有 LLM 调用、设备接收并返回成功的日志，尚缺物理动作观察；`wave_hand` 尚无调用与实物证据。

---

## C. 软件实现已完成（待实机验收）

### C1. 配网二维码 `overlay_qr`（计划一 Task 6/7）—— ✅ 已实现
设备进入配网模式后，左眼显示可扫二维码（内容 `WIFI:S:Xiaozhi-XXXX;T:nopass;;`），右眼显示青色等待环。

实现：vendored Nayuki qrcodegen（MIT、纯 C99）+ `OverlayQr` 编码层 + `OverlayRenderer` 白底黑码布局。二维码含四模块静默区；典型 25×25 模块在圆屏内使用 5px/module。

验证：主机测试通过；将固件编码器与渲染器输出交给 macOS Vision，可精确回读原始 Wi-Fi 载荷。初版已用 ESP-IDF 6.1 构建、烧录并在实机触发，日志确认 `Xiaozhi-7AC9` AP 启动及 `配网二维码已显示，side=25`。本轮又补了 overlay 并发保护，最新镜像构建通过但尚未重烧；仍需用户用手机确认真屏反光和对焦条件下可扫。

### C2. OTA 升级进度显示 —— ✅ 已实现
`Display::SetDownloadProgress()` 保留普通屏的文本进度语义，`EyeDisplay` 重写为双眼同步进度环；固件 OTA 与 assets 下载共用同一入口。失败分支会离开 `upgrading`，迟到的下载回调也会按状态丢弃，避免恢复眼睛后又被旧进度覆盖。主机渲染/升级恢复测试和最新固件构建通过，但尚未烧录本轮镜像；仍需一次真实 OTA/资源下载确认真屏进度推进及失败恢复。

---

## D. 需要硬件或网络环境配合

### D1. 舵机 trim 校准
`0°` 是电气中位，装到手臂上后「自然下垂」未必对应 0°。建议存 NVS `Settings` 以便在线调整而不重烧。

### D2. 独立 5V 电源 + 1000µF 电容
**实测**：USB 供电下双路舵机无间歇连续动作会 BROWNOUT 掉电重启（可复现两次）。目前靠动作间强制冷却 300ms 规避。装进玩偶后手臂带载，电流更大，独立电源成为必需。

### D3. 服务端固定地址
服务端 IP 随网络变化，症状是「设备突然不说话」。本次已为此绕了三轮。

**已选本地方案**：使用 Mac 的 Bonjour 名称 `MacBook-Pro-107.local`。本机验证该名称当前可解析到 `10.35.80.13`；固件构建配置已启用 `CONFIG_LWIP_DNS_SUPPORT_MDNS_QUERIES=y`，服务端也允许显式配置完整 WebSocket/视觉 URL。

迁移时把设备 NVS 中的 OTA 地址配为 `http://MacBook-Pro-107.local:8003/xiaozhi/ota/`，并把服务端 `data/.config.yaml` 的地址改为 `ws://MacBook-Pro-107.local:8010/xiaozhi/v1/` 与对应的 `:8003/mcp/vision/explain`。当前没有直接修改运行配置：设备仍需重新配网，且手机热点是否转发 mDNS 必须在现场验证；若热点隔离 mDNS，则改用路由器 DHCP 地址保留或公网域名。

### D5. MPR121 电极部位映射未定
**2026-09-10 实机接线**：接了 8 路（ELE1、2、4、6、7、9、10、11），未接 4 路（ELE0、3、5、8）。但**每个电极贴在玩具哪个部位尚未确定**，需后续决策。

在此之前有两处代码是错的，不要以为它们已经成立：

- `config.h` 的 `TOUCH_HEAD_ELECTRODE` 仍是 0，而 ELE0 根本没接线。这个常量目前只被诊断读数用到，还没有真正的语义。
- `PlushBehavior::OnTouch()` 对所有电极执行同一套反射（眼睛变 happy + 手臂欢呼）。只有一个电极时无所谓，8 个电极下必须先有部位语义才能设计行为。

决策时需要一并回答：哪些部位共用一种反应、哪些各自不同；多个电极同时被触摸（抱住玩具会同时压到多处）时如何取舍，是取第一个、取优先级最高的，还是合成一个「被抱住」的复合事件。

### D6. MPR121 的 ELE1 与 ELE2 无响应
**2026-09-10 实测**：这两路已接线，但 60 秒采样中从未被判定为触摸，最大偏移仅 27 和 11，而正常电极触摸时偏移 124 到 158。

症状形态是**恒定偏移而非触摸尖峰**：ch1 全程稳定低 14 到 27，ch2 稳定低 7 到 11，从第一秒持续到最后一秒，与是否触摸无关。说明这两路被持续加载 —— 引线碰到其他导体，或电极片压在金属、潮湿填充物上。芯片的基线跟踪会把恒定偏移吸收掉，因此不报触摸，手指再叠加也顶不出足够增量。

排查方向：先断开这两根引线单独量电极对地电容，再检查走线是否与舵机线或屏 SPI 线并行贴合。

### D4. MPU6050 六轴姿态传感器接入
手头已有模块，等 MPR121 触摸接入完成后再设计。**引脚可行性已确认**：MPU6050 走 I2C，地址 0x68，可并入 `I2C_NUM_0`，与 PCA9685(0x40)、MPR121(0x5A) 不冲突，只要不接 INT 引脚就不需要任何新 GPIO。待定的是姿态语义（抱起、摇晃、倒置、拍打）如何映射到眼睛与手臂，以及轮询频率与 I2C 带宽的取舍 —— 该总线还要同时喂 PCA9685 的舵机脉宽。

---

## E. 已知技术债

| 项 | 说明 |
|---|---|
| 引脚已用尽 | 无空闲 GPIO，再加外设需先腾地方（详见 `STATUS.md` 引脚表）。I2C 外设是例外：可并入 `I2C_NUM_0`，代价是放弃中断引脚、改为轮询 |
| 应用层控制台静默 | 2026-09-10 发现：`entry 0x403c88f4` 之后串口一个字节都没有，用官方 `esp-idf-monitor` 抓 30 秒复现，重新烧录后依旧，而设备 Wi-Fi 与 HTTP 全程正常。已排除：固件与本地 build 一致、控制台配置确为 USB-Serial-JTAG 且日志等级 INFO、代码无 `esp_log_set_vprintf` 或全局关日志、未开 PM/DFS、与舵机电源接反无关。后果是崩溃现场、启动失败信息和一切打日志的诊断全部拿不到，`diagnostics` 动作实际已失效。下一步应查 GPIO3（JTAG_SEL 启动脚，现作舵机 SCL）复位时的电平 |
| LVGL 仍被编译 | `main/CMakeLists.txt:20-31` 无条件加入 `SOURCES`，本板不用但仍链接。2026-09-08 依赖审计确认这不能靠删除几行源文件安全解决：`Display` 公共接口和 `assets.cc` 的资源解析仍直接使用 `EmojiCollection`/LVGL 类型。emoji 图片资源已不打包（flash 占用的大头），代码部分依赖链接器裁剪；若要彻底移除，应另立跨核心显示层重构任务，而不是混入本板交付 |
| SPI 保持 10MHz | 20MHz 是允许上限而非交付要求。10MHz 已在面包板双屏并联条件下稳定，用户接受当前表现并停止专项排查；只有未来重新观察到可见卡顿或错位才重开提速验证 |
| 计划文档与实现有偏差 | 计划二写的是 GPIO 直驱 PWM，实际改用 PCA9685；计划一 Task 2/3 已合并实现。文档中已就地标注，但未整体重写 |
